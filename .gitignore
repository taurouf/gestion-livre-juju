# --- Node / package managers ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json.* # (on garde le lock principal)
.pnpm-store/

# --- Next.js / Build output ---
.next/
out/
.next/cache/
.vercel/

# --- TypeScript / SWC / Babel caches (même si tu es en JS pur) ---
*.tsbuildinfo
.swc/
.cache/
.eslintcache

# --- Env & secrets (NE PAS commiter vos clés) ---
.env
.env.*            # .env.local, .env.production, etc.
!.env.example     # garde un exemple vide si tu en as un

# --- Supabase (garde le code, ignore les artefacts locaux éventuels) ---
supabase/.temp/
supabase/.branches/
supabase/.cache/

# --- OS / IDE ---
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo

# --- Tests / Coverage ---
coverage/
.nyc_output/

# --- Misc / outils ---
*.log
*.pid
*.seed
*.tgz
*.orig
*.rej

# --- Uploads temporaires (si tu en crées en local) ---
public/uploads/tmp/
